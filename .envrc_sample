command=op

if ! [ -x "$(command -v op)" ]; then
    command="op.exe"
fi

export ARM_SUBSCRIPTION_ID=XX
export ARM_CLIENT_ID=XX
export ARM_SUB_ID=XX
export ARM_TENANT_ID=XX
export ARM_CLIENT_SECRET=XX